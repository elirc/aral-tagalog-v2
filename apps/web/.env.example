# Development defaults to http://localhost:3001.
# Production defaults to /api (the supplied Caddy proxy routes this to Fastify).
# For a separately hosted API, set its public HTTPS URL BEFORE building.
# NEXT_PUBLIC_API_URL=https://api.example.com
