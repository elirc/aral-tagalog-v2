# Public API URL embedded in the mobile app. HTTPS is required for release builds.
# For development on a phone, use your computer's LAN IP instead of localhost.
EXPO_PUBLIC_API_URL=http://localhost:3001
